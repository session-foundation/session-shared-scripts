# What the deployment runs. Test-only dependencies live in requirements-dev.txt.
requests==2.32.3

# relay.py only: the Discord interactions endpoint.
fastapi==0.141.1
uvicorn==0.52.4
# Ed25519, for verifying Discord's request signatures.
pynacl==1.6.2
